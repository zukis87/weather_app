services:
  - type: web
    name: city-weather
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: 10000
      - key: VISUAL_CROSSING_API_KEY
        sync: false
